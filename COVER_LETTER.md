Hi,

RAG-backed semantic search for finding app entities, Text-to-SQL over a SaaS schema, and chart generation via a charting MCP server — built a working demo of this stack before reaching out: {VERCEL_URL}

Covers the Vectorize retrieval pipeline, natural-language-to-SQL with schema grounding, and Workers AI tool output — the pieces that matter most for delivering this independently.

Built an AI pipeline with Claude API and structured output that cut a 4-hour manual workflow to 20 minutes — same pattern as your toolchain.

One question: is your SaaS database a standard relational schema or does it use per-tenant isolation (separate schema per customer)?

10-minute call or I can scope the first milestone in a doc — your pick.

Humam
